export class User {
    constructor(
        public name: string,
        public family: string,
        public email: string,
        public birthDate: string,
        public address: string,
        public postalCode: string
    ) {
    }
}